# Deployment scripts
This folder contains scripts used to automatically configure, build, and deploy the entire pipeline via a CI/CD Pipeline managed by GitHub Actions.

The following dictionary below describes the function of each script.

# Scripts
`deploy-web-scraping-container`
- deploys the web scraping container....

`build-tag-push-artifact-registry`
- builds containers and pushes them to the artifact registry.


- more to be added..
